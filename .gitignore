.venv/
__pycache__/
*.py[cod]
.env
.pytest_cache/
.ruff_cache/
*.wav
data/
livekit/*.local.json

# Google service account keys. These are private keys to the appointment sheet,
# and the README tells you to download one into this directory.
service-account*.json
*service-account.json
gcp-*.json
